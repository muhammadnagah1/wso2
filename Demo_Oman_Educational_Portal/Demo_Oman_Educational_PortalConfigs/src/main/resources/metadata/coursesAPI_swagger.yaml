openapi: 3.0.1
info:
  title: coursesAPI
  description: API Definition of coursesAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/course
paths:
  /getCourse:
    get:
      responses:
        default:
          description: Default response
  /addCourse:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
